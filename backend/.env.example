# =============================================================================
# EVSE Simulator - Backend Environment Variables
# =============================================================================
# Copiez ce fichier en .env et remplissez les valeurs
# Ne jamais commiter le fichier .env avec des vraies credentials!
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
SERVER_PORT=8887
SPRING_PROFILES_ACTIVE=dev

# -----------------------------------------------------------------------------
# OCPP WebSocket URLs
# -----------------------------------------------------------------------------
OCPP_DEFAULT_URL=ws://localhost:8080/ocpp
OCPP_DEFAULT_ENV=local

# URLs par environnement (optionnel - valeurs par défaut fournies)
OCPP_TEST_URL=wss://evse-test.total-ev-charge.com/ocpp/WebSocket
OCPP_PP_URL=wss://evse-pp.total-ev-charge.com/ocpp/WebSocket
OCPP_PROD_URL=wss://evse.total-ev-charge.com/ocpp/WebSocket

# -----------------------------------------------------------------------------
# TTE Integration (Total Energies API)
# -----------------------------------------------------------------------------
TTE_ENABLED=true

# Cognito OAuth2 Credentials (OBLIGATOIRE pour TTE)
# Obtenez ces credentials auprès de l'équipe TTE
TTE_CLIENT_ID=your_cognito_client_id_here
TTE_CLIENT_SECRET=your_cognito_client_secret_here

# Cognito Token URL (optionnel - valeur par défaut fournie)
TTE_COGNITO_URL=https://tte-pool-prod.auth.eu-central-1.amazoncognito.com/oauth2/token

# TTE API URLs par environnement
TTE_API_URL=https://api.total-ev-charge.com
TTE_API_TEST_URL=https://evplatform.evcharge-test.totalenergies.com
TTE_API_PP_URL=https://evplatform.evcharge-pp.totalenergies.com

# -----------------------------------------------------------------------------
# CORS Configuration (Production)
# -----------------------------------------------------------------------------
# Liste des origines autorisées, séparées par des virgules
CORS_ORIGINS=https://your-frontend-domain.com

# -----------------------------------------------------------------------------
# Default Values
# -----------------------------------------------------------------------------
DEFAULT_CP_ID=CP001
DEFAULT_ID_TAG=TEST-TAG
DEFAULT_VEHICLE=renault-zoe

# -----------------------------------------------------------------------------
# Data Storage
# -----------------------------------------------------------------------------
DATA_PATH=./data

# -----------------------------------------------------------------------------
# Runner Configuration
# -----------------------------------------------------------------------------
RUNNER_BASE_URL=http://localhost:8877
