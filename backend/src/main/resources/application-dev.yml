# =============================================================================
# EVSE Simulator - Development Profile
# =============================================================================

server:
  port: 8887

spring:
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

# CORS permissif pour le développement
cors:
  allowed-origins:
    - "http://localhost:*"
    - "http://127.0.0.1:*"
    - "https://*.loca.lt"
    - "https://*.ngrok.io"
    - "https://*.ngrok-free.app"
  allowed-methods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS
    - PATCH
  allowed-headers: "*"
  allow-credentials: true
  max-age: 3600

# Performance réduite pour le dev
performance:
  max-sessions: 1000
  websocket-pool-size: 20
  async:
    core-pool-size: 10
    max-pool-size: 50
    queue-capacity: 1000

# Logging verbeux
logging:
  level:
    root: INFO
    com.evse.simulator: DEBUG
    org.springframework.web.socket: DEBUG
    org.springframework.messaging: DEBUG
    org.springframework.web: DEBUG
  pattern:
    console: "%clr(%d{HH:mm:ss.SSS}){faint} %clr(%-5level) %clr([%15.15thread]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %msg%n"

# Données locales
data:
  path: ./data
  auto-save-interval: 10000

# Swagger activé
springdoc:
  swagger-ui:
    enabled: true
    try-it-out-enabled: true

# =============================================================================
# TTE API Configuration (Cognito OAuth2) - DEV
# =============================================================================
tte:
  enabled: true
  cognito:
    client-id: 3tmbtgs4jcdf7f53uedn1eej11
    client-secret: j8bbm7tbq50ge8ueg3ag6abvbl1n8rt7g5hbdg8hulkp803olur
  api:
    # URLs TTE par environnement - sélection automatique selon l'URL CSMS de la session
    test-url: https://evplatform.evcharge-test.totalenergies.com
    pp-url: https://evplatform.evcharge-pp.totalenergies.com