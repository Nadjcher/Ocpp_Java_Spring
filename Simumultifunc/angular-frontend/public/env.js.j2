(function(window) {
  window['env'] = window['env'] || {};
  window['env']['portalSdkUrl'] = "{{ portal_sdk_url }}";
  window['env']['authentication'] = {
    authorizeUrl: "{{ cognito_authorization_endpoint }}",
    identityProviders: [
      {
        clientId: "{{ clientId['cognito_internal'] }}",
        displayedName: "{{ cognito_internal_displayed_name }}",
        id: "{{ cognito_internal_idp }}",
        userType: "{{ cognito_internal_user_type }}",
      },
      {
        clientId: "{{ clientId['cognito_external'] }}",
        displayedName: "{{ cognito_external_displayed_name }}",
        id: "{{ cognito_external_idp }}",
        userType: "{{ cognito_external_user_type }}",
      },
    ],
    redirectUri: "https://{{ external_totalevcharge_dns }}/gpm-simulator/login/callback",
  }
})(this);
